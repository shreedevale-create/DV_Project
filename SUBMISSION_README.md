Submission package contents — Chess at Scale

Included in the ZIP (`DV_Project_chess_submission.zip`):

- `DV_Project_.ipynb` — analysis notebook (Jupyter)
- `dashboard/` — Streamlit dashboard folder (includes `app.py`, `requirements.txt`, `Dockerfile`, `Procfile`, and `data/` with pre-aggregated CSVs)
- `dashboard/README.md` — run and deploy instructions
- `SUBMISSION_README.md` — this file (instructions + checklist)

Notes:
- The full raw dataset `chess_games.csv` is heavy and NOT included. Link to original dataset: https://www.kaggle.com/datasets/arevel/chess-games
- If your instructor requires the raw dataset file included, move `chess_games.csv` into the package before zipping.

Checklist to submit:
- [ ] Upload the ZIP `DV_Project_chess_submission.zip` to your submission portal
- [ ] Include a link to your GitHub repo (optional) containing the same files
- [ ] Include the live dashboard URL if deployed (Streamlit Cloud or other)

If you want, I can:
- Export the notebook to HTML/PDF within this workspace (requires `nbconvert` / LaTeX for PDF)
- Push the repo to GitHub and deploy to Streamlit Cloud and then embed the live link into the presentation
- Build the Docker image locally to verify runtime

Tell me which of those you'd like me to do next.